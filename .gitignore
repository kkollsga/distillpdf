# Rust
target/
**/target/
# Python
__pycache__/
*.pyc
.venv/
*.so
# Local-only deep gates, third-party PDF corpora, and competitor head-to-head all live
# under benchmarking/ (never published). The committed CI suite under tests/ is fully
# self-contained — every fixture is generated by us.
benchmarking/
# macOS
.DS_Store

# Local working notes (not shared)
CLAUDE.md
.build-venv/

# Local dev working folder + cross-project coordination channel — never committed.
# See dev-docs/README.md and inbox/README.md for the canonical layouts, and the
# dev-docs-cleanup / read-inbox skills for the auto-purged time-boxed tiers within.
/dev-docs/
/inbox/

# Python build artifacts
dist/

# Downloaded granite GGUF models (default --ocr-engine granite location)
ocr_model/

# Docs (mkdocs) local build
.docs-venv/
site/
