# Rust
target/
**/target/
Cargo.lock
# Python
__pycache__/
*.pyc
.venv/
*.so
# bench artifacts — out/ is a regenerated cache of distillpdf's own HTML.
tests/out/
# PDF corpora: kept locally for the gates, NOT published (third-party PDFs).
# Committed ground truth (bench/fixtures/, bench/*.json) + the synthetic
# bench/corpus_tables/ stay tracked so the gate logic is reproducible.
tests/corpus/
tests/corpus_headers/
# Private competitor benchmarking (head-to-head vs pymupdf4llm/pdf_oxide/etc.).
benchmarking/
# macOS
.DS_Store
